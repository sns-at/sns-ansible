- name: Patch control node (no reboot, but notify)
  hosts: control
  become: true
  tasks:
    - name: Upgrade packages on VM
      ansible.builtin.apt:
        update_cache: true
        upgrade: dist
        autoremove: true

    - name: Warn if reboot required
      ansible.builtin.stat:
        path: /var/run/reboot-required
      register: reboot_required_control

    - name: Print reboot-needed warning
      ansible.builtin.debug:
        msg: "Reboot required on this node, but not rebooting automatically."
      when: reboot_required_control.stat.exists

- name: Patch VMs
  hosts: vm
  become: true
  tasks:
    - name: Upgrade packages on VMs
      ansible.builtin.apt:
        update_cache: true
        upgrade: dist
        autoremove: true

    - name: Check if reboot required
      ansible.builtin.stat:
        path: /var/run/reboot-required
      register: reboot_required

    - name: Reboot if required
      ansible.builtin.reboot:
        msg: "Reboot triggered by Ansible after patching"
        reboot_timeout: 900
      when: reboot_required.stat.exists

- name: Patch GPU nodes
  hosts: gpu
  become: true
  tasks:
    - name: Upgrade packages on GPU nodes (conservative)
      ansible.builtin.apt:
        update_cache: true
        upgrade: yes
        autoremove: true

    - name: Warn if reboot required (no reboot here)
      ansible.builtin.stat:
        path: /var/run/reboot-required
      register: reboot_required_gpu

    - name: Reboot if required
      ansible.builtin.reboot:
        msg: "Reboot triggered by Ansible after pathing"
      when: reboot_required_gpu.stat.exists

